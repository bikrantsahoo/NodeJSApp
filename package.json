{
  "name": "hello-bikrant",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "test": "jest"  // <-- This tells npm to run Jest when you run npm test
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^27.0.0"
  }
}
